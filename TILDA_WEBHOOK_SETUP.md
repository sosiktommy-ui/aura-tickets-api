# Настройка Tilda Webhook для AURA Tickets API

## 📋 Обзор
Этот документ описывает настройку webhook в Tilda для автоматической обработки заказов билетов в системе AURA Tickets.

## 🔗 Endpoint для webhook
```
POST https://your-railway-app.railway.app/api/tilda/webhook
```

## 🛠️ Настройка в Tilda

### 1. Перейдите в настройки проекта Tilda
- Откройте ваш проект в Tilda
- Зайдите в "Settings" → "Additional" → "Webhooks"

### 2. Добавьте новый webhook
- URL: `https://your-railway-app.railway.app/api/tilda/webhook`
- Method: `POST`
- Content-Type: `application/x-www-form-urlencoded` или `application/json`

### 3. Настройте отправку данных
Убедитесь, что ваша форма отправляет следующие поля:

#### Обязательные поля:
- `orderid` - Уникальный ID заказа
- `name` - Имя покупателя
- `amount` - Сумма платежа
- `status` - Статус платежа (confirmed/paid/success)

#### Рекомендуемые поля:
- `email` - Email покупателя
- `phone` - Телефон покупателя
- `tranid` - ID транзакции

#### Дополнительные поля для билетов:
- `ticket_type` - Тип билета (VIP, Standard, Early Bird)
- `event_date` - Дата мероприятия (формат: DD.MM)
- `event_name` - Название мероприятия
- `city` - Город проведения
- `country` - Код страны (RU, KZ, BY)
- `club_id` - ID клуба/площадки
- `promocode` - Использованный промокод

## 📝 Пример данных, отправляемых в webhook

### JSON формат:
```json
{
    "orderid": "ORDER-12345",
    "tranid": "T-67890",
    "name": "Иван Иванов",
    "email": "ivan@example.com", 
    "phone": "+7900123456",
    "amount": "1500.00",
    "status": "confirmed",
    "ticket_type": "VIP",
    "event_date": "31.12",
    "event_name": "New Year Party",
    "city": "Moscow", 
    "country": "RU",
    "club_id": "1",
    "promocode": "SAVE20"
}
```

### Form-data формат:
```
orderid=ORDER-12345
name=Иван Иванов
email=ivan@example.com
phone=+7900123456
amount=1500.00
status=confirmed
ticket_type=VIP
event_date=31.12
event_name=New Year Party
city=Moscow
country=RU
club_id=1
promocode=SAVE20
```

## ✅ Ответы API

### Успешная обработка:
```json
{
    "status": "success",
    "order_id": "ORDER-12345", 
    "ticket_id": 42,
    "qr_token": "abc123def456"
}
```

### Пропуск заказа (неподтвержденный платеж):
```json
{
    "status": "skipped",
    "reason": "Payment status: pending"
}
```

### Ошибка:
```json
{
    "detail": "Missing required field: orderid"
}
```

## 🧪 Тестирование

### 1. Проверка работоспособности:
```bash
curl -X GET https://your-railway-app.railway.app/api/tilda/test
```

### 2. Тест webhook с примером данных:
```bash
curl -X POST https://your-railway-app.railway.app/api/tilda/test-webhook
```

### 3. Ручная отправка тестового заказа:
```bash
curl -X POST https://your-railway-app.railway.app/api/tilda/webhook \
  -H "Content-Type: application/json" \
  -d '{
    "orderid": "TEST-001",
    "name": "Test User",
    "email": "test@example.com",
    "amount": "1000.00",
    "status": "confirmed"
  }'
```

## 🔒 Безопасность

### Рекомендации:
1. **Проверка источника**: Добавьте проверку IP адресов Tilda
2. **Подпись запросов**: Используйте секретный ключ для подписи webhook
3. **HTTPS**: Обязательно используйте HTTPS для webhook URL
4. **Логирование**: Все запросы логируются для мониторинга

### Добавление проверки подписи (опционально):
```python
import hmac
import hashlib

def verify_tilda_signature(payload: bytes, signature: str, secret: str) -> bool:
    expected = hmac.new(secret.encode(), payload, hashlib.sha256).hexdigest()
    return hmac.compare_digest(signature, expected)
```

## 📊 Мониторинг

### Логи
Все webhook запросы логируются со следующей информацией:
- Время получения
- Данные заказа
- Результат обработки
- Ошибки (если есть)

### Проверка статуса
Используйте `/health` endpoint для проверки работоспособности API:
```bash
curl https://your-railway-app.railway.app/health
```

## 🚀 Деплой

После добавления кода:
1. Закоммитьте изменения в git
2. Railway автоматически задеплоит обновления
3. Проверьте работу через тестовые endpoints

## 💡 Дополнительные возможности

### Интеграция с ботом
После создания билета можно настроить отправку уведомлений в Telegram:
- QR-код билета
- Детали мероприятия  
- Инструкции для входа

### Аналитика
API предоставляет endpoints для аналитики продаж:
- `/api/stats/` - общая статистика
- `/api/history/` - история сканирований
- `/api/tickets/` - список билетов

## 🆘 Поддержка

В случае проблем:
1. Проверьте логи Railway
2. Используйте тестовые endpoints
3. Проверьте формат отправляемых данных
4. Убедитесь, что webhook URL доступен